# Microservices project for song discovery

## Getting started

To start the web application, execute the following command:

```sh
mvn tomcat7:run
```
The application is available at the url : http://localhost:8080/microservice/<a_number>

pipeline test
